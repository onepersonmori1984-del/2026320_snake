# Local Workflow

This file was created on the local filesystem first.
You can use `git status` or `git diff` locally to verify these changes before committing or pushing in the future!

This ensures you always have a chance to debug and verify locally.
